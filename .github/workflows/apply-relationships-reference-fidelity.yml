name: Apply relationships reference fidelity

on:
  push:
    branches:
      - fork/2.0.7-renderer-bundles
    paths:
      - '.github/workflows/apply-relationships-reference-fidelity.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-test-build:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fork/2.0.7-renderer-bundles

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.14.3'

      - name: Apply reference-fidelity patch
        run: python3 tools/apply_relationships_reference_fidelity.py

      - name: Verify source diff
        run: git diff --check

      - name: Test and build release APK
        shell: bash
        run: |
          set -euo pipefail
          chmod +x android/gradlew
          android/gradlew -p android :app:testDebugUnitTest :app:assembleRelease --stacktrace --console=plain

      - name: Commit verified source changes
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/apply-relationships-reference-fidelity.yml
          rm tools/apply_relationships_reference_fidelity.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add android/app/src/main/java/io/github/retrofrost/cts/android/RendererBundle.kt \
                  android/app/src/main/java/io/github/retrofrost/cts/android/RelationshipsPrecisionFrameRenderer.kt \
                  .github/workflows/apply-relationships-reference-fidelity.yml \
                  tools/apply_relationships_reference_fidelity.py
          git commit -m 'renderer: add final relationships reference fidelity layer'
          git push origin HEAD:fork/2.0.7-renderer-bundles

      - name: Package APK
        shell: bash
        run: |
          cp android/app/build/outputs/apk/release/app-release.apk Cubical-Compare-2.0.7-Relationships-Reference-Fidelity.apk
          sha256sum Cubical-Compare-2.0.7-Relationships-Reference-Fidelity.apk > Cubical-Compare-2.0.7-Relationships-Reference-Fidelity.apk.sha256

      - uses: actions/upload-artifact@v4
        with:
          name: Cubical-Compare-2.0.7-Relationships-Reference-Fidelity
          path: |
            Cubical-Compare-2.0.7-Relationships-Reference-Fidelity.apk
            Cubical-Compare-2.0.7-Relationships-Reference-Fidelity.apk.sha256
          if-no-files-found: error
          retention-days: 14
