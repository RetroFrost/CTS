name: Restore tested CTS 2.0 exact reference motion

# Re-run the source-measured reference pass on demand and after workflow updates.

on:
  push:
    branches: [android]
    paths:
      - ".github/workflows/restore-measured-timeline.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  exact-reference-motion:
    runs-on: ubuntu-latest
    steps:
      - name: Check out Android history
        uses: actions/checkout@v4
        with:
          ref: android
          fetch-depth: 0

      - name: Install frame-measured opening motion and exact palette
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import re
          path = Path("android/app/src/main/java/io/github/retrofrost/cts/android/timeline/TimelineEngine.kt")
          text = path.read_text()
          replacement = '''    private val malesPhasePullKeys = arrayOf(
        528f to 0.000000f, 529f to 0.000000f, 530f to 0.008333f, 531f to 0.008333f,
        532f to 0.008333f, 533f to 0.016667f, 534f to 0.016667f, 535f to 0.016667f,
        536f to 0.025000f, 537f to 0.025000f, 538f to 0.033333f, 539f to 0.033333f,
        540f to 0.041667f, 541f to 0.041667f, 542f to 0.041667f, 543f to 0.050000f,
        544f to 0.050000f, 545f to 0.058333f, 546f to 0.066667f, 547f to 0.066667f,
        548f to 0.075000f, 549f to 0.075000f, 550f to 0.083333f, 551f to 0.091667f,
        552f to 0.091667f, 553f to 0.100000f, 554f to 0.108333f, 555f to 0.116667f,
        556f to 0.116667f, 557f to 0.125000f, 558f to 0.133333f, 559f to 0.133333f,
        560f to 0.141667f, 561f to 0.150000f, 562f to 0.150000f, 563f to 0.158333f,
        564f to 0.166667f, 565f to 0.175000f, 566f to 0.183333f, 567f to 0.183333f,
        568f to 0.200000f, 569f to 0.200000f, 570f to 0.208333f, 571f to 0.216667f,
        572f to 0.225000f, 573f to 0.233333f, 574f to 0.241667f, 575f to 0.250000f,
        576f to 0.250000f, 577f to 0.258333f, 578f to 0.266667f, 579f to 0.275000f,
        580f to 0.283333f, 581f to 0.291667f, 582f to 0.300000f, 583f to 0.308333f,
        584f to 0.316667f, 585f to 0.325000f, 586f to 0.333333f, 587f to 0.333333f,
        588f to 0.350000f, 589f to 0.358333f, 590f to 0.366667f, 591f to 0.375000f,
        592f to 0.375000f, 593f to 0.391667f, 594f to 0.391667f, 595f to 0.408333f,
        596f to 0.416667f, 597f to 0.416667f, 598f to 0.433333f, 599f to 0.433333f,
        600f to 0.450000f, 601f to 0.458333f, 602f to 0.458333f, 603f to 0.475000f,
        604f to 0.475000f, 605f to 0.491667f, 606f to 0.500000f, 607f to 0.500000f,
        608f to 0.516667f, 609f to 0.516667f, 610f to 0.533333f, 611f to 0.533333f,
        612f to 0.541667f, 613f to 0.550000f, 614f to 0.558333f, 615f to 0.566667f,
        616f to 0.575000f, 617f to 0.583333f, 618f to 0.591667f, 619f to 0.600000f,
        620f to 0.608333f,
    )'''
          text, count = re.subn(r"    private val malesPhasePullKeys = arrayOf\(.*?\n    \)\n    const val MALES_BODY_SECONDS", replacement + "\n    const val MALES_BODY_SECONDS", text, count=1, flags=re.S)
          if count != 1:
              raise SystemExit("malesPhasePullKeys block not found")
          text = text.replace("private const val MALES_STEADY_START_SHIFT = 0.614439f", "private const val MALES_STEADY_START_SHIFT = 0.608333f", 1)
          path.write_text(text)
          monitor = Path("android/app/src/main/java/io/github/retrofrost/cts/android/ui/ProgramMonitor.kt")
          m = monitor.read_text()
          m = m.replace("Color(0xFFF2F2F2)", "Color(0xFFF0F0F0)")
          m = m.replace("Color(0xFF635E57)", "Color(0xFF625F58)")
          monitor.write_text(m)
          PY

      - name: Verify exact-reference invariants
        shell: bash
        run: |
          set -euo pipefail
          FILE=android/app/src/main/java/io/github/retrofrost/cts/android/timeline/TimelineEngine.kt
          MONITOR=android/app/src/main/java/io/github/retrofrost/cts/android/ui/ProgramMonitor.kt
          git diff --check
          grep -q '528f to 0.000000f' "$FILE"
          grep -q '535f to 0.016667f' "$FILE"
          grep -q '620f to 0.608333f' "$FILE"
          grep -q 'MALES_STEADY_START_SHIFT = 0.608333f' "$FILE"
          grep -q 'Color(0xFFF0F0F0)' "$MONITOR"
          grep -q 'Color(0xFF625F58)' "$MONITOR"
          grep -q 'Color(0xFFE8E6E2)' "$MONITOR"
          grep -q 'Color(0xFF181818)' "$MONITOR"

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Compile exact-reference pass
        shell: bash
        run: |
          set -o pipefail
          gradle --project-dir android :app:assembleDebug --stacktrace --console=plain 2>&1 | tee android/gradle-exact-reference.log

      - name: Run unit tests
        shell: bash
        run: |
          set -o pipefail
          gradle --project-dir android :app:testDebugUnitTest --stacktrace --console=plain 2>&1 | tee android/gradle-exact-reference-tests.log

      - name: Commit only tested exact-reference changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add android/app/src/main/java/io/github/retrofrost/cts/android/timeline/TimelineEngine.kt android/app/src/main/java/io/github/retrofrost/cts/android/ui/ProgramMonitor.kt
          if git diff --cached --quiet; then
            echo "Exact-reference changes already present"
          else
            git commit -m "Use frame-measured CTS reference motion and palette"
            git fetch origin android
            git rebase origin/android
            git push origin HEAD:android
          fi

      - name: Upload debug APK and validation logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: CTS-2.0-exact-reference-debug
          path: |
            android/app/build/outputs/apk/debug/*.apk
            android/gradle-exact-reference.log
            android/gradle-exact-reference-tests.log
          if-no-files-found: ignore
