name: Apply 2.0.5 thumbnail export

on:
  workflow_dispatch:
  push:
    branches:
      - feature/thumbnails-megapack-2.0.5
    paths:
      - tools/apply_205_thumbnail_poppins.py
      - .github/workflows/apply-2.0.5-thumbnail-export.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Apply Poppins thumbnail changes
        run: python tools/apply_205_thumbnail_poppins.py
      - name: Commit source changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add android tests
          git diff --cached --quiet && exit 0
          git commit -m 'Render thumbnails with bundled Poppins'
          git push origin HEAD:feature/thumbnails-megapack-2.0.5
