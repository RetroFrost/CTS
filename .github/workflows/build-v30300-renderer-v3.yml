name: Build Cubical Compare 3.0.300 Renderer v3
# Recovery build: compile the verified baseline and apply only scoped Renderer v3 fixes.

on:
  push:
    branches:
      - fork/2.0.7-renderer-bundles
    paths:
      - .github/workflows/build-v30300-renderer-v3.yml
      - tools/fix_v30300_renderer_v3_identity.py
      - tools/fix_v30300_export_reader_compat.py
      - tools/fix_renderer_v3_coordinate_space.py
      - tools/fix_renderer_v3_project_data.py
      - android/app/src/main/java/io/github/retrofrost/cts/android/RendererV3ProjectData.kt
      - android/app/src/test/java/io/github/retrofrost/cts/android/RendererV3CompatibilityTest.kt
      - android/app/src/test/java/io/github/retrofrost/cts/android/RendererV3ProjectDataContractTest.kt
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-v30300-renderer-v3
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fork/2.0.7-renderer-bundles
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Verify 3.0.300 Renderer v3 contract
        run: python tools/fix_v30300_renderer_v3_identity.py

      - name: Restore 3.0.300 export-reader compatibility
        run: python tools/fix_v30300_export_reader_compat.py

      - name: Fix Renderer v3 coordinate-space composition
        run: python tools/fix_renderer_v3_coordinate_space.py

      - name: Wire Renderer v3 to live project data
        run: python tools/fix_renderer_v3_project_data.py

      - name: Unit test compatibility and build APKs
        working-directory: android
        shell: bash
        run: |
          set -euxo pipefail
          chmod +x gradlew
          ./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease --stacktrace

      - name: Stage named APKs and checksums
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p dist
          cp android/app/build/outputs/apk/debug/app-debug.apk dist/Cubical-Compare-3.0.300-Renderer-v3-DATA-DRIVEN-debug.apk
          cp android/app/build/outputs/apk/release/app-release.apk dist/Cubical-Compare-3.0.300-Renderer-v3-DATA-DRIVEN-release.apk
          (cd dist && sha256sum *.apk > SHA256SUMS.txt)

      - name: Upload APK bundle
        uses: actions/upload-artifact@v4
        with:
          name: Cubical-Compare-3.0.300-Renderer-v3-DATA-DRIVEN
          path: dist/*
          if-no-files-found: error

      - name: Persist corrected Renderer v3 runtime
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add \
            android/app/src/main/java/io/github/retrofrost/cts/android/RendererBundle.kt \
            android/app/src/main/java/io/github/retrofrost/cts/android/RendererImportActivity.kt \
            android/app/src/main/java/io/github/retrofrost/cts/android/RendererV3FrameRenderer.kt \
            android/app/src/main/java/io/github/retrofrost/cts/android/RendererBridge.kt \
            android/app/src/main/java/io/github/retrofrost/cts/android/RendererProjectGuard.kt \
            android/app/src/main/java/io/github/retrofrost/cts/android/ExportService.kt \
            android/app/src/test/java/io/github/retrofrost/cts/android/RendererV3CompatibilityTest.kt
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'Fix Renderer v3 project-data rendering and compatibility UI'
          git push origin HEAD:fork/2.0.7-renderer-bundles
