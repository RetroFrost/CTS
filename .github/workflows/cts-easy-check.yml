name: CTS Easy check

on:
  pull_request:
    paths:
      - "comparison_studio/**"
      - "tests/**"
      - "requirements.txt"
      - ".github/workflows/cts-easy-check.yml"
  push:
    branches:
      - feature/cts-easy

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Compile Python sources
        run: python -m compileall -q comparison_studio run.py

      - name: Check normal-speed intro
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -p "test_easy_timing.py" -k custom_target -v

      - name: Check scroll-only retiming
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -p "test_easy_timing.py" -k only_scroll -v

      - name: Check no-scroll projects
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -p "test_easy_timing.py" -k target_is_ignored -v

      - name: Check visual settings wrapper
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -p "test_easy_timing.py" -k visual_settings -v

      - name: Run complete unit suite
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -v
